<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Perfil</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    /* ===== RESET BÁSICO ===== */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      min-height: 100vh;
      background: #1c1c1c;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      color: #ffffff;
    }

    /* ===== FRAME ===== */
    .frame {
      width: 460px;
      background: #2a2a2a;
      border-radius: 14px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
      overflow: hidden;
    }

    /* ===== BANNER ===== */
    .banner {
      height: 150px;
      background-image: url("banner.jpg"); /* <-- cambia esto */
      background-size: cover;
      background-position: center;
      position: relative;
    }

    /* ===== AVATAR ===== */
    .avatar {
      position: absolute;
      bottom: -48px;
      left: 20px;
      width: 96px;
      height: 96px;
      background-image: url("avatar.jpg"); /* <-- cambia esto */
      background-size: cover;
      background-position: center;
      border-radius: 6px;
      border: 3px solid #2a2a2a;
    }

    /* ===== CONTENIDO ===== */
    .content {
      padding: 64px 20px 20px;
    }

    .name {
      font-size: 22px;
      font-weight: 600;
      margin-bottom: 4px;
    }

    .subtitle {
      font-size: 14px;
      color: #b5b5b5;
      margin-bottom: 12px;
    }

    .bio {
      font-size: 14px;
      line-height: 1.5;
      color: #e0e0e0;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 520px) {
      .frame {
        width: 92%;
      }
    }
  </style>
</head>

<body>

  <div class="frame">
    <div class="banner">
      <div class="avatar"></div>
    </div>

    <div class="content">
      <div class="name">Tu Nombre</div>
      <div class="subtitle">Desarrollador • Discord • Roblox</div>
      <div class="bio">
        Texto corto de biografía.  
        Algo simple, directo y limpio.
      </div>
    </div>
  </div>

</body>
</html>
